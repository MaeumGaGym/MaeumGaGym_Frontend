import { Modal } from '@/app/@auth/(.)login/Modal'
import { LoginModal } from '@/components/LoginModal'

export default function AuthModal() {
  return (
    <Modal>
      <LoginModal />
    </Modal>
  )
}
