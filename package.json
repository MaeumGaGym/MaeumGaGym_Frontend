{
  "name": "maeumgagym",
  "version": "0.0.0",
  "packageManager": "yarn@3.7.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "service/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:client": "yarn workspace @service/client build",
    "build:webview": "yarn workspace @service/webview build",
    "doc": "typedoc"
  },
  "devDependencies": {
    "prettier": "^3.2.2",
    "turbo": "^1.11.3",
    "typedoc": "^0.25.13",
    "typedoc-material-theme": "^1.0.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "next": "^14.0.4",
    "typedoc-plugin-merge-modules": "^5.1.0"
  }
}
